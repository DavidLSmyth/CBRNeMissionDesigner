package GPSUtils;
import org.geotools.*;
//An interface that any GPSCoordinate class must adhere to 
import org.osgeo.proj4j.CoordinateReferenceSystem;
public interface CoordinateInterface {

	public double getMetresToOther(CoordinateInterface coord);
	Coordin
	
}
